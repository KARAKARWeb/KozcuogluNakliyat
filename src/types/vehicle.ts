export interface Vehicle {
  id: string;
  name: string;
  type: string;
  capacity: string;
  description: string;
  image: string;
  features: string[];
  isActive: boolean;
  order: number;
  createdAt: string;
  updatedAt: string;
}
